#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include "main.h"

/**
* main - Entry point
*
* Return: 0 on success, error code otherwise
*/
int main(void)
{
/* long long int res = INT_MAX * 2;
_printf("%d + %d = %d\n", INT_MAX, INT_MAX, res); */
return (0);
}
